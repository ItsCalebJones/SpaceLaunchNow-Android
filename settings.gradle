include ':mobile', ':data', ':news', ':common', ':spacestations', ':events', ':astronauts', ':starship'
buildCache {
    local {
        // Set local build cache directory.
        directory = "${settingsDir}/build-cache"
    }
}