include ':mobile', ':wear', ':data', ':news', ':common', ':spacestations', ':events', ':astronauts'
buildCache {
    local {
        // Set local build cache directory.
        directory = "${settingsDir}/build-cache"
    }
}